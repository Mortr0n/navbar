import React from 'react';
import Form from './Form';
// I don't need context on this.  just passing through here
const FormWrapper = (props) => {

    return(
        <Form/>
    )
}

export default FormWrapper;